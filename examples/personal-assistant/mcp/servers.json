{
  "servers": [
    {
      "id": "filesystem",
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "${HOME}"],
      "auth": {
        "type": "none"
      },
      "tools": {
        "include": "all"
      },
      "description": "Local filesystem MCP server"
    }
  ]
}
